import 'package:flutter/material.dart';

class PrivacyConsent extends StatelessWidget {
  const PrivacyConsent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Privacy consent')),
    );
  }
}
